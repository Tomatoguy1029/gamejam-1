[gd_scene format=3 uid="uid://b8wta4plwg7b4"]

[sub_resource type="GDScript" id="GDScript_8fud6"]
script/source = "extends Control

@onready var main_label:   Label  = $VBox/MainLabel
@onready var hint_label:   Label  = $VBox/HintLabel
@onready var retry_button: Button = $VBox/RetryButton
@onready var hide_timer:   Timer  = $HideTimer

func _ready():
	GameManager.phase_changed.connect(_on_phase_changed)
	GameManager.game_over.connect(_on_game_over)
	retry_button.pressed.connect(_on_retry_pressed)
	hide()

func _on_phase_changed(new_phase: int):
	retry_button.hide()
	match new_phase:
		GameManager.Phase.START:
			var costs = _get_cost_hint()
			if GameManager.wave_count == 0:
				main_label.text = \"PLACE SOLDIERS!!\"
				hint_label.text = \"%s  |  Click to place  |  Enter: start\" % costs
				hide_timer.stop()
				show()
			else:
				# wave クリア後は WaveResult パネルが先に出るので、
				# PhaseMessage は hint のみ・少し遅れて表示
				main_label.text = \"\"
				hint_label.text = \"%s  |  Click to place  |  Enter: next wave\" % costs
				hide_timer.stop()
				show()
		GameManager.Phase.PLAYING:
			main_label.text = \"WAVE %d  —  DEFEND!\" % GameManager.wave_count
			hint_label.text = \"Space: toggle gate\"
			show()
			hide_timer.start(2.5)

func _on_game_over(is_win: bool):
	hide_timer.stop()
	retry_button.show()
	if is_win:
		main_label.text = \"CLEAR!!\"
		hint_label.text = \"All waves cleared. Congratulations!\"
	else:
		main_label.text = \"GAME OVER\"
		hint_label.text = \"The main base has fallen.\"
	show()

func _get_cost_hint() -> String:
	var uc = GameManager.UNIT_CONFIG
	var a = uc.get(\"ally_attacker\")
	var t = uc.get(\"ally_tank\")
	var ar = uc.get(\"ally_archer\")
	var ac = a.cost if a else 0
	var tc = t.cost if t else 0
	var arc = ar.cost if ar else 0
	return \"1: 攻撃兵 %dG  2: 守備兵 %dG  3: 弓兵 %dG\" % [ac, tc, arc]

func _on_retry_pressed():
	get_tree().reload_current_scene()

func _on_hide_timer_timeout():
	hide()
"

[sub_resource type="LabelSettings" id="LabelSettings_qqn4i"]
font_size = 60
font_color = Color(0, 0, 0, 1)

[sub_resource type="LabelSettings" id="LabelSettings_uqueh"]
font_size = 30
font_color = Color(0, 0, 0, 1)

[sub_resource type="InputEventAction" id="InputEventAction_6k1am"]
action = &"start_game"

[sub_resource type="Shortcut" id="Shortcut_26ovk"]
events = [SubResource("InputEventAction_6k1am")]

[node name="PhaseMessage" type="Control" unique_id=400000001]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
mouse_filter = 2
script = SubResource("GDScript_8fud6")

[node name="VBox" type="VBoxContainer" parent="." unique_id=400000002]
layout_mode = 1
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
offset_left = -419.5
offset_top = -317.0
offset_right = 420.5
offset_bottom = -173.0
grow_horizontal = 2
grow_vertical = 2
theme_override_constants/separation = 8
metadata/_edit_group_ = true

[node name="MainLabel" type="Label" parent="VBox" unique_id=400000003]
layout_mode = 2
theme_override_font_sizes/font_size = 48
label_settings = SubResource("LabelSettings_qqn4i")
horizontal_alignment = 1

[node name="HintLabel" type="Label" parent="VBox" unique_id=400000004]
layout_mode = 2
theme_override_font_sizes/font_size = 16
label_settings = SubResource("LabelSettings_uqueh")
horizontal_alignment = 1

[node name="RetryButton" type="Button" parent="VBox" unique_id=400000006]
visible = false
custom_minimum_size = Vector2(200, 50)
layout_mode = 2
theme_override_font_sizes/font_size = 20
shortcut = SubResource("Shortcut_26ovk")
text = "↩  RETRY"

[node name="HideTimer" type="Timer" parent="." unique_id=400000005]
one_shot = true

[connection signal="timeout" from="HideTimer" to="." method="_on_hide_timer_timeout"]
